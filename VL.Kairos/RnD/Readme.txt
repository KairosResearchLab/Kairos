CustomViewers
  Shows how to patch custom viewers used for the tool tip. The custom viewer feature must be unlocked via command line argument: vvvv.exe --custom-viewers

PreviewSelection
  Shows how to listen to events coming from the VL patch editor UI and use that information to build a viewer/editor for a process node.

PinGroups
  Shows how to create pin groups and add pins to them via the VL API